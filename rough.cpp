#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n = 8;
    // float res = sqrt(((n * (n + 1)) / 2));
    float res=10.345;
    float dec=res-int(res);
    cout<<dec;
    // if(floor(res)==0) cout<<res;
    // else cout<<-1;
    return 0;
}